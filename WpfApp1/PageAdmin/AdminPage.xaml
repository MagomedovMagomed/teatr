<Page x:Class="WpfApp1.PageAdmin.AdminPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp1.PageAdmin"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="AdminPage" IsVisibleChanged="Page_IsVisibleChanged">

    <Grid Background="#FF9A273E">
        <Grid.RowDefinitions>
            <RowDefinition Height="450*"></RowDefinition>
            <RowDefinition Height="80*"></RowDefinition>
        </Grid.RowDefinitions>

        <Button Content="Добавить Спектакль" Click="Add_Click" Name="Add" HorizontalAlignment="Left" Margin="10,5,0,0" Grid.Row="1" VerticalAlignment="Top" Height="50" Width="164" Foreground="#FF9A273E" Background="#FFEEF195" FontSize="15" FontWeight="Bold"/>
        <Button Grid.Column="3" Click="Delete_Click" Content="Удалить" Name="Delete" HorizontalAlignment="Right" Margin="0,5,10,0" Grid.Row="1" VerticalAlignment="Top" Height="50" Width="100" Foreground="#FF9A273E" Background="#FFEEF195" FontWeight="Bold" FontSize="20"/>
        <TabControl Grid.Row="0" Foreground="#FF9A273E">
            <TabItem Header="Спектакль" Grid.Column="0" Foreground="#FF9A273E">
                <Grid Background="#FF9A273E">
                    <DataGrid x:Name="DBSpec" AutoGenerateColumns="False" IsReadOnly="True" Background="#FF9A273E">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Название спектакля" Binding="{Binding Nazvanie}" Width="*" Foreground="#FFEEF195"></DataGridTextColumn>
                            <DataGridTextColumn Header="Продолжительность" Binding="{Binding Time}" Width="*" Foreground="#FFEEF195"></DataGridTextColumn>
                            <DataGridTextColumn Header="Осталось билетов" Binding="{Binding Ostatok}" Width="*" Foreground="#FFEEF195"></DataGridTextColumn>
                            <DataGridTextColumn Header="Постановщик" Binding="{Binding Postanovshik.Familia}" Width="*" Foreground="#FFEEF195"></DataGridTextColumn>
                            <DataGridTextColumn Header="Художник" Binding="{Binding Pointer.Familia}" Width="*" Foreground="#FFEEF195"></DataGridTextColumn>
                            <DataGridTextColumn Header="Сценарист" Binding="{Binding Screenwriter.Familia}" Width="*" Foreground="#FFEEF195"></DataGridTextColumn>
                            <DataGridTextColumn Header="Постер" Binding="{Binding Poster}" Width="*" Foreground="#FFEEF195"></DataGridTextColumn>
                            <DataGridTextColumn Header="Жанр" Binding="{Binding Zanr.Name}" Width="*" Foreground="#FFEEF195"></DataGridTextColumn>
                            <DataGridTemplateColumn Width="auto">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button Content="Редактировать" Name="Edit" Click="Edit_Click"></Button>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>
            <TabItem Header="User" Foreground="#FF9A273E">
                <Grid Background="#FF9A273E">
                    <DataGrid x:Name="DBUser" AutoGenerateColumns="False" IsReadOnly="True" Background="#FF9A273E">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Логин" Binding="{Binding Login}" Width="*" Foreground="#FFEEF195"></DataGridTextColumn>
                            <DataGridTextColumn Header="Пароль" Binding="{Binding Password}" Width="*" Foreground="#FFEEF195"></DataGridTextColumn>
                            <DataGridTextColumn Header="Роль" Binding="{Binding idRole}" Width="*" Foreground="#FFEEF195"></DataGridTextColumn>
                            <DataGridTextColumn Header="Фамилия" Binding="{Binding Surename}" Width="*" Foreground="#FFEEF195"></DataGridTextColumn>
                            <DataGridTextColumn Header="Имя" Binding="{Binding Name}" Width="*" Foreground="#FFEEF195"></DataGridTextColumn>
                            <DataGridTextColumn Header="Отчество" Binding="{Binding Father_name}" Width="*" Foreground="#FFEEF195"></DataGridTextColumn>
                            <DataGridTextColumn Header="Телефон" Binding="{Binding Teleph}" Width="*" Foreground="#FFEEF195"></DataGridTextColumn>
                            <DataGridTextColumn Header="Дата рождения" Binding="{Binding Data_Birth}" Width="*" Foreground="#FFEEF195"></DataGridTextColumn>
                            <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="*" Foreground="#FFEEF195"></DataGridTextColumn>
                            <DataGridTemplateColumn Width="auto">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button Content="Редактировать" Name="Edit" Click="Edit_Click_2"></Button>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>
        </TabControl>
        <Button Content="Добавить Пользователя" Click="Edit_Click_1" x:Name="Edit" HorizontalAlignment="Left" Margin="295,5,0,0" Grid.Row="1" VerticalAlignment="Top" Height="50" Width="196" Foreground="#FF9A273E" Background="#FFEEF195" FontWeight="Bold" FontSize="15"/>
    </Grid>
</Page>
